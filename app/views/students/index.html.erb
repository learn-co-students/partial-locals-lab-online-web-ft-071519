<h3> Search for a student: </h3>
<%= form_tag students_path, :method => "get" do %>
  <%= text_field_tag :query, "Search for a student"%>
  <%= submit_tag "Search" %>
<% end %>

  <%= @students.each do |s| %>
    <%= render partial: 'student', locals: {student: s}%>
  <% end %>